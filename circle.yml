machine:
  services:
    - docker

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - fig run --no-deps web
    - curl --retry 10 --retry-delay 5 -v http://localhost:80
    - python code/tests.py